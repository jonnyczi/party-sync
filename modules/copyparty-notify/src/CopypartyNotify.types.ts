export type CopypartyNotifyModuleEvents = Record<string, never>;

/** Maps to NotificationManagerCompat.IMPORTANCE_* on the native side. */
export type NotifyImportance = 'min' | 'low' | 'default' | 'high';
