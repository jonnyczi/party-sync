# Roadmap / deferred work

Handoff list of known, deliberately-deferred work. Each item is written so a fresh
session (or contributor) can pick it up cold. The release pipeline itself
(GitHub Releases via Dagger) is **done and green** — see
[`release-pipeline.md`](./release-pipeline.md) for how it works.

Status legend: 🟢 done · 🟡 partially done / blocked on a prerequisite · ⚪ not started.

---

## 1. Cleartext-HTTP blocked in release builds 🟢 (fixed & verified)

**Resolved** by `plugins/withCleartextTraffic.js` (`usesCleartextTraffic=true`,
shipped v0.1.0) and re-verified at runtime during the 2026-07-04/05
background-sync verification sessions: release APKs sync fine against the plain
`http://` dockerized copyparty. Original notes kept below for context.

## 1a. (original notes) Cleartext-HTTP blocked in release builds

**Symptom.** A **release** APK can't talk to plain `http://` copyparty servers:
every request fails with **"Network request failed"** (e.g. "5 scanned, 0
uploaded, 5 failed"). Debug/dev builds work, because Expo/RN dev builds set
`usesCleartextTraffic=true` — so this only bites the *published* flavor (GitHub
Releases, F-Droid, Play). Many copyparty deployments are plain `http://` on a LAN,
so real users will hit this. Discovered 2026-06-28 while smoke-testing on the
emulator against the dockerized copyparty at `http://10.0.2.2:3923`.

**Why it's not yet fixed.** It needs a product decision (how permissive to be) —
not just code. Pick one with the user:
- Broadest/simplest: `expo.android.usesCleartextTraffic: true` in `app.json`
  (re-enables cleartext app-wide; least safe).
- Safer: a custom `network_security_config.xml` injected via a config plugin
  (mirror `plugins/withReleaseSigning.js`) — allow cleartext only for user-added
  hosts, or default-allow with a per-server opt-in.
- Or push users to `https://` (copyparty supports TLS) and surface a clear in-app
  error when a cleartext request is blocked.

**Key constraint.** `android/` is gitignored and regenerated by `expo prebuild`,
so the manifest/network-config change **must** flow through `app.json` or an Expo
config plugin — never hand-edit `AndroidManifest.xml`.

**How to verify a fix.** Build a **release** APK (`dagger call build-apk`), install
it, and retry a sync against an `http://` server (the dockerized copyparty at
`http://10.0.2.2:3923` from the emulator). The upload should succeed instead of
"Network request failed".

---

## 2. F-Droid submission 🟡 (recipe ready, blocked on the repo being private)

The de-Googling is done (zero Firebase/GMS — `scripts/scan-nonfree-apk.py` gates
both `ci.yml` and, since v0.11.0, the **signed** artifact in `release.yml`), and
the recipe at
[`metadata/io.github.jonnyczi.copypartyclient.yml`](../metadata/io.github.jonnyczi.copypartyclient.yml)
is pinned at the current release (`0.11.0` / `versionCode 13` / `commit: v0.11.0`).

**The one hard blocker: `github.com/jonnyczi/party-sync` is PRIVATE.** F-Droid
clones anonymously, so nothing below can proceed until it is public. (Going
public also stops Actions minutes being billed and unlocks branch protection.)

What's left, in order:

1. **Make the repo public.** Note this is no longer a "mirror" — party-sync is
   `origin`, and the `v0.11.0` tag the recipe pins already exists there.
2. **Add `phoneScreenshots/`.** F-Droid reads
   `fastlane/metadata/android/en-US/images/phoneScreenshots/`, which does not
   exist yet. The ten scenes live in `docs/screenshots/` as `.webp`; copy the
   light-theme set across. F-Droid accepts WebP, but PNG is the safer bet.
   (`title.txt`, `short_description.txt` and `full_description.txt` **are**
   already present — an earlier revision of this file wrongly listed them as
   missing.)
3. **Validate locally before the MR.** Install `fdroidserver` and run
   `fdroid build io.github.jonnyczi.copypartyclient`. Expo on F-Droid's Debian
   buildserver is fiddly — expect to iterate. Known rough edges, also noted in
   the recipe's header comment:
   - **No `ndk:` pin**, so F-Droid picks its own; `flake.nix` builds against
     `27.1.12297006`.
   - **`npm ci` installs devDependencies**, including `better-sqlite3`'s
     prebuilt `.node` binary — exactly what F-Droid's scanner flags. Try
     `--omit=dev` if `expo prebuild` still succeeds without them.
   - **The nodesource `curl | bash`** may draw reviewer objection; Debian's own
     Node is too old for Expo SDK 54, so it needs a defensible answer.

   The recipe already overrides `gradle.properties` to ship ARM-only, matching
   the GitHub/Play artifacts.
4. **Prove the build is reproducible** — required by the `Binaries:` decision in
   §2a, and the largest unknown here. Start by pinning `NIX_IMAGE`
   (`.dagger/src/index.ts:44`) to a digest, then build one tag twice and
   `diffoscope` the results.
5. **Submit** the recipe as a merge request to `gitlab.com/fdroid/fdroiddata`.

### 2a. Signing: ship our own binary via `Binaries:` (decided 2026-08-24)

**Decision: F-Droid publishes *our* signed APK, not one signed with F-Droid's
key.** The default behaviour — F-Droid stripping our signature and re-signing —
would make an F-Droid install *not* cross-updatable with the GitHub build:
switching sources would mean uninstall + reinstall, losing `file_state` and
forcing a full re-upload. `Binaries:` avoids that. F-Droid builds from source,
compares its output against the APK we published, and on a match ships our
binary, so both channels carry the same signature.

Recipe changes this implies (already applied):

```yaml
Binaries: https://github.com/jonnyczi/party-sync/releases/download/v%v/party-sync-v%v.apk
AllowedAPKSigningKeys: f687d5f0de1f584476a604663609c9b4df45bae74c9c846e15082740092eb77f
```

The `%v` template matches our published asset naming (`party-sync-vX.Y.Z.apk`),
and the key is the cert verified on v0.11.0 (`CN=jonnyczi, O=copyparty-client`).
The old "F-Droid strips and re-signs with its own key regardless" comment in the
recipe no longer applies and has been removed.

**What's already in our favour:**

- **ABIs match.** `scripts/ci-android-build.sh:29` pins
  `armeabi-v7a,arm64-v8a`, the same pair the recipe writes into
  `gradle.properties`. A mismatch here would make byte-equality impossible.
- **The toolchain is fully pinned** by `flake.nix` + the committed `flake.lock`
  (nixpkgs `c7f47036…`): JDK 17, build-tools 35.0.0/36.0.0, NDK
  27.1.12297006, CMake 3.22.1.
- **The release build already runs inside `nix develop`** in a container, so it
  does not depend on host state.

**What has to be fixed or proven:**

1. **Pin the base image.** `.dagger/src/index.ts:44` is
   `NIX_IMAGE = "nixos/nix:latest"` — a mutable tag, with a TODO already sitting
   next to it. Until it is pinned to a digest, "reproducible" means nothing.
2. **F-Droid builds on Debian, not our Nix container.** Their stock toolchain
   will not coincidentally produce our bytes. The tractable path is to have the
   recipe install Nix and build through the same committed `flake.lock` (reusing
   `scripts/ci-android-build.sh`), so F-Droid runs *our* build rather than a
   lookalike. Expect reviewer scrutiny — it is a large download and wants
   network during the build — and be ready for it to be refused, in which case
   the fallback is matching Debian's toolchain to the flake by hand.
3. **The usual RN/Android reproducibility hazards**: Metro bundle determinism,
   zip entry ordering and timestamps, and any build path or timestamp baked into
   the native `.so` files or `BuildConfig`.

**How to prove it before submitting:** build the same tag twice and compare —
`dagger call build-apk` twice, then `diffoscope` the two APKs. Only once that is
clean is it worth testing against the published artifact. Note the earlier
observation that v0.11.0's APK matched v0.10.0's byte *size* is weak evidence at
best; equal size is not equal bytes.

**Ongoing cost, accepted:** verification runs per release, so a toolchain drift
silently stops F-Droid publishing new versions until it is chased down. And
because the signing key's password is unrecoverable (§4), we can never re-sign
or rotate — this signature is now permanent.

---

## 3. Google Play (Phase 3) ⚪ (not started)

Needs **user prerequisites first** (account + console setup — Claude can't do these):
- Play Console account ($25), create the app under `io.github.jonnyczi.copypartyclient`.
- Enrol in **Play App Signing**; complete Data Safety + privacy policy.
- Create a **service-account JSON** with release permissions (store as a CI secret).

Then the implementation:
- Build the **AAB** via `dagger call build-aab` (already implemented; signs with the
  upload key from the `UPLOAD_*` secrets). `release.yml` already builds + uploads
  the AAB as a release asset.
- Automate submission: a `fastlane supply` lane or the `r0adkll/upload-google-play`
  action driven by the service-account secret — ideally wrapped as a Dagger function
  `submitPlay` for CI portability, else a small job appended to `release.yml`. Start
  with the **internal** track and pass the pre-launch checks.
- **Listing art is ready**: `fastlane/metadata/android/en-US/images/icon.png`
  (512²) and `featureGraphic.png` (1024×500), both Play-mandatory, come from
  `npm run gen-brand`. Phone screenshots still need copying/converting out of
  `docs/screenshots/` (Play wants PNG/JPEG, not WebP).

---

## 4. Canonical mirror is behind 🟡 (housekeeping)

The project moved to a new GitHub repo and the history was rewritten, so the
remote layout has changed again. As of now: `origin` is
**`github.com/jonnyczi/party-sync`**, tracked by **`main`** (renamed from
`master`), where CI, releases and the signing secrets live. `copyparty-client`
is the previous GitHub repo, and `selfhosted` is `dev.jonnyczi-systems.com`.

Both secondary remotes still hold the **pre-rewrite** history. Every commit was
re-dated during the migration, so their histories have genuinely **diverged**
from `main` — pushing to either now requires `--force`, not a fast-forward, and
would rewrite whatever is published there. Left undone deliberately;
`copyparty-client` keeps serving the v0.1.0–v0.10.0 release downloads.

**Do not delete `copyparty-client`, or its `UPLOAD_*` secrets.** The upload
keystore's password is **unrecoverable** (established 2026-08-24: created
non-interactively inside a session whose transcript the 30-day retention default
deleted; absent from shell history and from every password store on the machine).
It now survives only as an encrypted GitHub Actions secret in two places —
`party-sync` and `copyparty-client` — so the old repo is the only backup copy.
Consequences: local signed builds are impossible (CI is the only thing that can
sign), backing up `upload.keystore` alone is useless without the password, and
losing both secret stores means no existing install can ever update in place.
The secrets were copied across on 2026-08-24 without anyone reading the value,
via a throwaway workflow in the owning repo; see `release-pipeline.md`.
Cert identity to verify against: SHA-256 `f687d5f0de1f5844…092eb77f`,
`CN=jonnyczi, O=copyparty-client`.

---

## Deferred: copyparty handshake 422 on partial-upload dedup collision ⚪

up2k dedups by content hash server-wide. When the only server-side copy of some
content is a **partial** upload (left behind by an interrupted run), uploading
identical content to any *other* folder fails `handshake 422` — and retries fail
identically forever. Surfaced nicely in the run UI, but there is no recovery.
Deliberately deferred from the background-sync-reliability branch (2026-07-04):
needs copyparty protocol research (a dedup-bypass/force flag on handshake, or
detect-and-resume of the partial in its original location) and possibly an
upstream issue. Repro + evidence in the 2026-07-04 verification session
(`tmp/background-sync-verification-report.md`, defect 6). The foreground-service
work makes stranded partials much rarer, so severity is reduced.

---

## Done (for context) 🟢

- **App permissions screen** (2026-08-09, `feat/app-permissions-view`): sibling
  of `/background-sync` at `/permissions`, reached from Settings → Privacy. One
  row per app-wide permission (photos, photo location, notifications) with an
  explicit required/optional level — camera-roll access is only *required* when
  a media job actually exists — and, for the optional ones, a plain statement of
  what leaving them off costs. Buttons ask in-app first and only fall back to
  system settings once Android stops prompting. A second section covers per-job
  SAF folder grants, which are not app-wide permissions and can be lost silently.
  Two correctness fixes rode along: Android 14 "Select photos" partial access is
  now distinguished from full access (`getMediaReadState`), and the notification
  check is now `POST_NOTIFICATIONS` **and** a new native
  `areNotificationsEnabled()` probe — the permission alone reads as granted while
  the user has the app's notifications switched off wholesale. The notifications
  row moved off the background-sync checklist, which is now five rows.
- **Background sync reliability + setup guidance** (2026-07-04,
  `feat/background-sync-reliability`): real dataSync foreground service + wake
  lock + RN headless keep-alive around every run (swipe-away and screen-off
  safe); custom WorkManager worker replaces expo-background-task
  (`setForeground` before JS, task defined in the bundle entry `index.ts` —
  route modules never execute headless, liveness watchdog for wedged headless
  spawns); health checklist screen (`/background-sync`) + dashboard card +
  job-form interception with one-tap fixes incl. the
  `ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` dialog; Data Saver probe
  un-stubbed; notification permission asked at first manual run.
- Reproducible Dagger release build (APK/AAB) in a `nixos/nix` container; only
  Docker needed locally. `lint` / `test` / `build-apk` / `build-aab`.
- GitHub Actions: `ci.yml` (lint+test always; heavy build on PR/dispatch) and
  `release.yml` (signed APK+AAB + `SHA256SUMS` on tag `v*`).
- De-Googled notifications (`modules/copyparty-notify`), release signing via
  `plugins/withReleaseSigning.js`, single-source versioning (`scripts/bump-version.mjs`).
- CI made to fit GitHub's single ~72 GB runner disk: nix toolchain cache, ARM-only
  release builds, aggressive disk cleanup. See `release-pipeline.md` → Known issues.
